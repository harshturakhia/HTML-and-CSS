#include<iostream>
using namespace std;

#define n 100

void sort(stack<int> &st)
{
    if(st.empty())
    {
        return;
    }

    int ele = st[top];

    while(! st.empty())
    {
        for(int i=0; i<st.size(); i++)
        {
            if(ele > )
        }
    }    
}


int main()
{
    stack<int> st;
    
    st.push(1);
    st.push(2);
    st.push(3);
    st.push(4);
}